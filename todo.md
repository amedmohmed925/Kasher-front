# Kasher Frontend Fix Todo

- [x] فحص عنوان الباك إند `https://kasher-project.vercel.app/` ومسارات الصحة وSwagger والتأكد من الاستجابة — العنوان يعيد Vercel `500 FUNCTION_INVOCATION_FAILED` حالياً.
- [x] فحص ملفات المشروع الحالية وتحديد سبب بقاء `Home.tsx` التجريبية ظاهرة — الصفحة كانت قالب Example غير مستبدل.
- [x] إضافة إعداد API base URL متوافق مع الباك إند وإدارة أخطاء الشبكة.
- [x] استبدال صفحة Example بواجهة Kasher فعلية RTL ومتجاوبة.
- [x] إضافة شاشة دخول وتجربة لوحة التاجر ونقطة البيع ببيانات العرض، مع إبقاء تنفيذ login الفعلي للمرحلة التالية بعد استقرار الباك إند.
- [x] بناء نموذج جلسة يحفظ role ويطبق حماية admin وsuperAdmin.
- [x] تفعيل تنقل وصفحات admin الخاصة بالمتجر فقط.
- [x] تفعيل تنقل وصفحات superAdmin الخاصة بإدارة النظام والتجار والاشتراكات.
- [x] منع الوصول إلى الصفحات والإجراءات غير المسموحة حتى لو كتب المستخدم الرابط مباشرة.
- [x] اختبار عزل البيانات والتنقل لكل دور.
- [x] تحويل عناصر تنقل admin إلى مسارات وصفحات مستقلة بمحتوى مختلف.
- [x] تحويل عناصر تنقل superAdmin إلى مسارات وصفحات مستقلة بمحتوى مختلف.
- [x] إضافة حالة الصفحة الحالية وعودة واضحة للوحة الرئيسية.
- [x] اختبار أن المعاينتين لا تعرضان نفس المحتوى — تم التحقق عبر بنية routes ومكونات المحتوى المنفصلة، ونجح build.
- [x] استخراج layout مشترك يحافظ على sidebar أثناء تغيير صفحات admin.
- [x] استخراج layout مشترك يحافظ على sidebar أثناء تغيير صفحات superAdmin.
- [x] اختبار بقاء sidebar والعنصر النشط عند التنقل — نجح TypeScript وbuild، وتم تثبيت sidebar داخل مكونات الأقسام.
- [x] إصلاح تمرير section من Wouter بدلاً من window.location.
- [x] تعريف routes صريحة لكل صفحة في admin وsuperAdmin.
- [x] اختبار أن كل رابط يفتح محتواه الصحيح وليس صفحة الفواتير — تم تثبيت section صراحة داخل كل Route ونجح TypeScript وbuild.
- [x] فحص الاستجابات الحقيقية للـ endpoints بعد عودة الباك إند، بما فيها root وlogin contract وroutes المحلية.
- [x] تفعيل login والتوكن وlogout وتجديد الجلسة إن كان مدعوماً — تم حفظ accessToken وrefreshToken وإرسال Authorization Bearer.
- [x] ربط بيانات admin الحقيقية الأساسية: إحصاءات لوحة المتجر والمنتجات والفواتير.
- [x] ربط بيانات superAdmin الحقيقية الأساسية: إحصاءات النظام وعدد المستخدمين والأرباح والمنتجات.
- [x] إزالة بيانات المعاينة من صفحات المنتجات والفواتير واستبدالها بحالات تحميل وحالة فارغة من API.
- [ ] اختبار التكامل النهائي بحساب admin وحساب superAdmin حقيقيين يقدمهما المستخدم.
- [x] إعادة تحليل routes/controllers/models الخاصة بالـ admin من الباك إند المنشور والمحلي.
- [x] إعادة جرد صفحات ومكونات الفرونت إند الحالية ونقاط الاستبدال.
- [x] توثيق عقد admin endpoints والـ request/response models داخل adminApi.ts ومطابقة routes/controllers.
- [x] تفعيل المصادقة الحقيقية والـ token والملف الأساسي للتصنيفات داخل طبقة المنتجات.
- [x] تفعيل المنتجات والمخزون: جلب، بحث محلي، إضافة FormData، وحذف.
- [x] تفعيل قائمة فئات admin من `/api/admin/categories` — endpoint أعاد HTTP 200 بحساب admin.
- [x] تنفيذ إضافة فئة وتعديلها وحذفها مع تحديث الحالة عبر createCategory/updateCategory/deleteCategory.
- [x] جعل اختيار الفئة في نموذج المنتج إلزامياً ومبنياً على البيانات الحقيقية.
- [x] اختبار قراءة دورة الفئات قبل إضافة منتج؛ لم يتم إنشاء بيانات اختبارية أو حذف بيانات حقيقية.
- [x] معالجة استجابة `/api/admin/customers/stats/overview` المتداخلة قبل عرضها في React عبر استخراج `data.overview`.
- [x] اختبار صفحة `/admin/customers` بعد الإصلاح من خلال مطابقة response وتشغيل TypeScript وBuild؛ لا يتم تمرير object إلى JSX.
- [x] تحديث عقد Customer API ليدعم data.customers وpagination وcustomer detail.
- [x] إضافة search/status/page/limit إلى قائمة العملاء في طبقة API.
- [x] تنفيذ نموذج تعديل العميل مع address/status.
- [x] إضافة تحقق client-side لتحديث Profile وكلمة المرور.
- [x] إضافة واجهة تعديل العميل وربطها بـ PUT /api/admin/customers/:id.
- [x] إضافة تفاصيل العميل وربطها بـ GET /api/admin/customers/:id.
- [x] إضافة pagination/search/status filters إلى جدول العملاء.
- [x] مطابقة responses الفعلية للإحصائيات والتقارير والملف الشخصي على الباك إند المنشور.
- [x] تفعيل `/api/admin/stats` و`/api/admin/reports` و`/api/admin/dashboard/analytics` داخل لوحة التحليلات.
- [x] تصحيح فلاتر التحليلات لاستخدام `/api/admin/dashboard/analytics?startDate=...&endDate=...` بدلاً من المسار القديم.
- [x] تحويل نتيجة التحليلات المفلترة من JSON الخام إلى بطاقات مؤشرات وجدول تقرير مفهوم.
- [x] إضافة فترة مقارنة ثانية وجلب بياناتها من نفس endpoint.
- [x] عرض فروقات المؤشرات ونسب التغير بين الفترتين.
- [x] إضافة تصدير Excel منسق للتقرير المقارن.
- [x] إضافة تصدير PDF منسق للتقرير المقارن.
- [x] تحسين رسائل التحميل وعدم وجود بيانات وفشل التصدير.
- [x] حفظ واستعادة آخر فلاتر التحليل من sessionStorage.
- [x] إضافة تصدير التقارير اليومية التفصيلية إلى Excel.
- [x] تحسين PDF بعناوين وتنسيق واضح؛ تم استخدام عناوين لاتينية ثابتة لضمان عدم تشوه النص مع خط jsPDF الافتراضي.
- [x] إضافة تصدير التقارير اليومية التفصيلية إلى PDF.
- [x] إضافة حفظ واستعادة قالب التقرير المفضل للمستخدم.
- [x] تحسين توضيح دعم اللغة العربية في PDF؛ تم استخدام عناوين لاتينية ثابتة مع إبقاء واجهة التحكم والتقارير عربية لتجنب تشوه النص بخط jsPDF الافتراضي.
- [x] جلب Profile الحالي داخل لوحة التحليلات لاستخدام بيانات المتجر في التصدير.
- [x] إضافة اسم الشركة والعنوان والهاتف إلى Excel وPDF.
- [x] تجهيز عنوان التقرير من اسم الشركة الحقيقي؛ الشعار لم يُدرج لعدم توفر logo URL ضمن Profile الحالي.
- [x] مطابقة endpoints تعديل المنتجات مع الباك إند؛ endpoint التفاصيل المنفصل يعيد 404 حالياً.
- [x] إضافة نموذج تعديل المنتج inline وربطه بـ PUT المنتجات؛ تفاصيل المنتج المنفصلة معلقة حتى نشر route صحيح.
- [x] إعادة اختبار تحليلات dashboard المفلترة؛ endpoint أعاد HTTP 200 بالفترة المطلوبة.
- [x] تفعيل `/api/admin/profile` GET وPUT داخل صفحة الملف الشخصي.
- [x] تصحيح واجهة جلب الأدمن بالـ ID لاستخدام `/api/admin/admin/:id`.
- [x] إضافة فلاتر startDate وendDate وإظهار نتيجة التحليل المتقدم أو رسالة الخطأ من الباك إند.
- [x] توضيح ربط كل صفحة admin بالـ endpoint داخل `admin-endpoints-map.md` وداخل واجهة التحليلات.
- [x] عرض dashboard analytics حسب response الفعلي: revenue/products/invoices/customers/inventory.
- [x] عرض reports كمصفوفة يومية مع topProducts دون إضافة مخططات أو بيانات مصطنعة.
- [x] إضافة route مستقل `/admin/profile` لصفحة Profile وجلب بيانات الأدمن الحقيقي.
- [x] اختبار جلب الأدمن بالـ ID على المسار الجديد؛ GET أعاد HTTP 200.
- [x] اختبار PUT profile؛ الطلب الاختباري الآمن بجسم فارغ أعاد HTTP 200 دون تغيير بيانات.
- [x] إضافة واجهة تعديل بيانات الأدمن وتجربة PUT profile بحساب الاختبار؛ GET أعاد 200 وPUT بجسم فارغ أعاد 200 دون تغيير مقصود.
- [x] تحديث نموذج إضافة العميل ليشمل email وaddress وstatus وإرسال body الكامل.
- [x] تحويل قائمة العملاء إلى جدول RTL منظم بأعمدة واضحة وحالات متجاوبة.
- [x] تنفيذ حذف العميل وربط detail API في طبقة التكامل؛ واجهة detail ستأتي مع المرحلة التالية.
- [x] تصحيح استخراج قائمة العملاء من `data.customers` بدلاً من افتراض مصفوفة مباشرة.
- [x] تصحيح قراءة إحصاءات overview المسطحة (`totalCustomers`, `activeCustomers`, `newCustomers`).
- [x] اختبار مطابقة الاستجابة وتشغيل TypeScript وBuild؛ قائمة العملاء ستُقرأ من `data.customers` والإحصاءات من `data.overview`.
- [x] عرض إحصاءات overview الأساسية دون تمرير الكائنات مباشرة إلى JSX.
- [x] توحيد مفاتيح تخزين accessToken وrefreshToken وuser في جلسة واحدة.
- [x] تنفيذ refresh-token عند 401 وإعادة الطلب الأصلي مرة واحدة فقط.
- [x] منع refresh requests المتزامنة عبر refreshInFlight promise واحدة.
- [x] تفعيل logout الحقيقي ومسح التوكنات عند النجاح أو الفشل.
- [x] اختبار login وendpoint محمي فعلياً؛ `/api/admin/stats` و`/api/admin/customers/stats/overview` أعادا HTTP 200. refresh/logout مفعّلان في الكود دون إنهاء جلسة الاختبار الحالية.
- [x] تفعيل العملاء والفواتير ونقطة البيع: جلب العملاء، إضافة/حذف، جلب الفواتير، وإنشاء فاتورة.
- [x] تفعيل التحليلات والإحصاءات واختبار حساب admin الحقيقي؛ endpoints الأساسية أعادت HTTP 200.
- [x] اختبار البناء والعرض على سطح المكتب والهاتف، ثم حفظ checkpoint جديد.

## مراجعة متطلبات pasted_content.txt
- [x] مقارنة endpoints التحليلات الجديدة مع `adminApi.ts` والصفحة الحالية.
- [x] فحص توفر `/api/admin/analytics/periods` للمصروفات وحساب الربح؛ HTTP 200.
- [x] فحص توفر `/api/admin/analytics/compare` للمقارنة الرسمية بين الفترات؛ HTTP 200.
- [x] فحص توفر `/api/admin/analytics/dashboard-summary` لملخص لوحة التحكم؛ HTTP 404 على الباك إند المنشور.
- [x] فحص توفر `/api/admin/analytics/dashboard` للتحليلات الشاملة؛ HTTP 200.
- [x] تطبيق الواجهات التي تعيد بيانات حقيقية فقط، وتوثيق endpoints غير المتاحة أو التي تعيد 404.
- [x] تشغيل TypeScript وBuild وحفظ checkpoint بعد اكتمال التعديل؛ نجح TypeScript وBuild.

## تحديثات الفواتير والرسم البياني
- [x] مطابقة `createInvoice` مع POST `/api/admin/invoices` وعقد الخصومات الجديد.
- [x] إضافة اختيار نوع الخصم وقيمته وحسابه في نقطة البيع.
- [x] إضافة طريقة الدفع والملاحظات إلى نموذج الفاتورة.
- [x] إضافة رسم بياني حقيقي من التقارير اليومية المتاحة.
- [x] اختبار العقد والبناء وتوثيق النتائج؛ نجح TypeScript وBuild، ولم يتم إنشاء فاتورة اختبارية حتى لا تتغير بيانات المتجر الحقيقية.

## استكمال نقطة البيع والتقارير
- [x] تحميل العملاء الحقيقيين داخل نقطة البيع واختيار customerId.
- [x] إرسال customerId في عقد إنشاء الفاتورة.
- [x] إضافة معاينة واضحة للفاتورة قبل الإرسال.
- [x] عرض رسم المصروفات من expensesByCategory أو حالة عدم توفر البيانات.
- [x] تشغيل TypeScript وBuild وحفظ checkpoint؛ نجح TypeScript وBuild بعد إصلاح استيرادات الرسم البياني.

## Comprehensive frontend audit and refactor
- [x] جرد structure والملفات ونقاط الدخول والـ routes والـ contexts والـ API layer.
- [x] مراجعة correctness/security/data integrity/stability والمشاكل الحرجة.
- [x] مراجعة الأنواع، استخدام `any`، response normalization، وإدارة الأخطاء؛ تم تحسين ApiError وHome types، وما زالت بعض analytics responses تحتاج DTOs أوسع.
- [x] مراجعة حجم المكونات، فصل المسؤوليات، التكرار، وserver state؛ تم تنفيذ code splitting وإزالة بيانات SuperAdmin الوهمية، وما زال AdminSection يحتاج تفكيكاً أوسع.
- [x] مراجعة الأداء، code splitting، التبعيات، والوصولية؛ أضيف lazy loading للمسارات وتحسينات aria أساسية، مع بقاء تحذير chunk كبير في AdminSection.
- [x] إنشاء تقرير Audit قبل التعديل وتحديد خطة التغيير في `refactor-audit.md`.
- [x] تطبيق إصلاحات عالية الأولوية: ErrorBoundary آمن، Guard بلا navigation أثناء render، Home بلا بيانات منتجات ثابتة، وApiError موحد.
- [x] تشغيل `pnpm check` و`pnpm build` بنجاح؛ لا توجد ملفات tests آلية، وتم توثيق المتبقي والمخاطر في `refactor-audit.md`.
