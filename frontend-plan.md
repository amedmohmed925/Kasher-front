# خطة بناء Frontend لنظام Kasher POS

## 1. الملخص التنفيذي

KasherProject هو نظام كاشير متعدد التجار مبني على Node.js/Express وMongoDB، ويقسّم الوصول إلى دورين رئيسيين: **admin** للتاجر وإدارة متجره، و**superAdmin** لإدارة المنصة والتجار والاشتراكات. الواجهة المقترحة ستكون React 19 + TypeScript داخل قالب Manus Web Static، بواجهة RTL عربية أولاً، مع طبقة API مركزية تتعامل مع JWT وبيانات الاشتراك والصلاحيات دون لمس الباك إند.

الهدف في النسخة الأولى هو توفير تجربة تشغيل سريعة للمسارات اليومية: تسجيل الدخول والتحقق، فتح لوحة التاجر، البحث عن المنتجات وإتمام البيع، إدارة المنتجات والفئات، متابعة الفواتير والعملاء، ثم التقارير والتحليلات. بعد ذلك تُضاف شاشات السوبر أدمن والاشتراكات.

> **قرار التصميم:** تم اختيار اتجاه «دفتر السوق الهادئ»: حداثة وظيفية دافئة، RTL أصيل، خلفية ورقية عاجية، كحلي حِبري، ونحاسي Kasher `#B96F4A` كعلامة مميزة. التفاصيل موثقة في `ideas.md`.

## 2. ما تم فهمه من الباك إند

المسارات مركبة في `server.js` كالتالي:

| المجال | Base path | الصلاحية | ملاحظات الواجهة |
|---|---|---|---|
| المصادقة | `/api/auth` | عامة | تسجيل، دخول، OTP، استعادة كلمة المرور، خروج، refresh token |
| إدارة التاجر | `/api/admin` | `admin` + اشتراك فعال | المنتجات، الفئات، الإحصاءات، الملف الشخصي، التقارير، التحليلات |
| الفواتير | `/api/admin/invoices` | `admin` + اشتراك فعال | إنشاء فاتورة، البحث، القائمة، الربح |
| العملاء | `/api/admin/customers` | `admin` + اشتراك فعال | CRUD وإحصاءات العملاء |
| المخزون | `/api/inventory` | `admin` + اشتراك فعال | المنتجات، البحث، الإحصاءات، التقرير، المنخفض المخزون |
| الاشتراكات | `/api/subscriptions` | حسب العملية | رفع إيصال للتاجر، قائمة واعتماد للسوبر أدمن |
| السوبر أدمن | `/api/superAdmin` | `superAdmin` | المستخدمون، الإحصاءات، الاشتراكات، الإشعارات، منتجات وفواتير جميع التجار |
| الصحة | `/api/health` | عامة | فحص الاتصال والبيئة |

يوجد اختلاف يجب أخذه في الاعتبار قبل الإنتاج: بعض ملفات الاختبارات تستخدم صيغاً مثل `/api/register` و`/api/super-admin/...`، بينما `server.js` يركّب المسارات الحالية تحت `/api/auth` و`/api/superAdmin`. سنعتمد على المسارات الفعلية في `server.js`، ونضع كل المسارات في ملف واحد قابل للتعديل، مع اختبار تكامل مبكر للتأكد من بيئة الباك إند المستخدمة.

## 3. خريطة الـ API التي ستبنى عليها طبقة التكامل

### المصادقة والحساب

| العملية | Endpoint | طريقة الاستخدام في الواجهة |
|---|---|---|
| التسجيل | `POST /api/auth/register` | نموذج إنشاء حساب تاجر |
| الدخول | `POST /api/auth/login` | حفظ access token وبيانات المستخدم |
| التحقق | `POST /api/auth/verify-otp` | خطوة OTP بعد التسجيل/إعادة الإرسال حسب العقد الحالي |
| نسيت كلمة المرور | `POST /api/auth/forgot-password` | إرسال طلب الاستعادة |
| إعادة التعيين | `POST /api/auth/reset-password` | نموذج كلمة المرور الجديدة |
| الخروج | `POST /api/auth/logout` | إلغاء الجلسة محلياً وإبلاغ الباك إند |
| تجديد التوكن | `POST /api/auth/refresh-token` | interceptor عند 401 مرة واحدة فقط |

### لوحة التاجر والمنتجات والفئات

| العملية | Endpoint | البيانات الأساسية |
|---|---|---|
| الإحصاءات | `GET /api/admin/stats` | KPIs للوحة التحكم |
| الفئات | `GET/POST/PUT/DELETE /api/admin/categories` و`/:id` | اسم الفئة ومعرفها |
| المنتجات | `GET/POST/PUT/DELETE /api/admin/products` و`/:id` | `name`, `sku`, `originalPrice`, `sellingPrice`, `quantity`, `categoryId`, `description`, `image` |
| بحث المنتجات | `GET /api/admin/products/search` | `q`, `category`, `minPrice`, `maxPrice` |
| الملف الشخصي | `GET/PUT /api/admin/profile` | بيانات الشركة والتاجر |

إضافة المنتج قد تستخدم `multipart/form-data` بسبب `upload.single('image')`، لذلك ستكون طبقة الطلبات قادرة على إرسال JSON أو FormData دون تكرار منطق المصادقة.

### المبيعات والفواتير والعملاء

| العملية | Endpoint | الاستخدام |
|---|---|---|
| إنشاء فاتورة | `POST /api/admin/invoices/add-invoices` | نقطة البيع والسلة والدفع |
| قائمة الفواتير | `GET /api/admin/invoices` | جدول مع فلاتر وتصفح |
| البحث | `GET /api/admin/invoices/search` | `customerName`, `price`, `date` |
| ربح فاتورة | `GET /api/admin/invoices/profit/:id` | تفاصيل هامش الربح |
| إجمالي الأرباح | `GET /api/admin/invoices/profits` | ملخص تقارير |
| العملاء | `GET/POST /api/admin/customers` | قائمة وإضافة عميل |
| عميل محدد | `GET/PUT/DELETE /api/admin/customers/:id` | صفحة تفاصيل وتعديل |
| إحصاءات العملاء | `GET /api/admin/customers/stats/overview` | مؤشرات العملاء |

### المخزون والتحليلات

| العملية | Endpoint | الاستخدام |
|---|---|---|
| إحصاءات المخزون | `GET /api/inventory/stats` | إجمالي المنتجات والقيمة والتنبيهات |
| تقرير المخزون | `GET /api/inventory/report` | `startDate`, `endDate`, `category` |
| منخفض المخزون | `GET /api/inventory/low-stock` | `threshold` قابل للتغيير |
| تحليلات لوحة التاجر | `GET /api/admin/dashboard/analytics` | مخططات الاتجاهات |
| ملخص لوحة التحكم | `GET /api/admin/analytics/summary` | بطاقات الأرقام |
| حسب الفترة | `GET /api/admin/analytics/periods` | يومي/أسبوعي/شهري |
| مقارنة | `GET /api/admin/analytics/compare` | مقارنة فترتين |
| تقرير عام | `GET /api/admin/reports` | تصدير/عرض التقرير حسب الاستجابة |

### السوبر أدمن والاشتراكات

تشمل الواجهة الإدارية العليا قائمة الأدمنز، إنشاء وتعديل وحذف أدمن، تفاصيل أدمن، إحصاءات المنصة، إدارة الاشتراكات، إرسال الإشعارات، وعرض المنتجات والفواتير عبر جميع التجار أو لتاجر محدد. كما سيستخدم التاجر `POST /api/subscriptions/subscriptions/upload` لرفع إيصال الاشتراك، بينما يعتمد السوبر أدمن الطلب عبر `POST /api/subscriptions/:subscriptionId/approve` مع `status` و`rejectionReason` عند الرفض.

## 4. المعمارية المقترحة للـ Frontend

### طبقات التطبيق

| الطبقة | المسؤولية | التنفيذ المقترح |
|---|---|---|
| Routing | حماية الصفحات وتقسيم الأدوار | Wouter مع `ProtectedRoute` و`RoleRoute` |
| API client | base URL، JWT، الأخطاء، refresh | Axios instance في `client/src/lib/api.ts` |
| Auth state | المستخدم، التوكن، الاشتراك، logout | `AuthContext` مع persistence آمن قدر الإمكان |
| Domain hooks | جلب/تحديث البيانات | hooks منفصلة مثل `useProducts`, `useInvoices`, `useAnalytics` |
| Schemas | تحقق نماذج الطلبات | Zod + React Hook Form، مع رسائل عربية |
| UI primitives | الحوارات، الجداول، الحالات | shadcn/ui الموجود في القالب |
| Feedback | loading/error/success | Skeletons، empty states، Sonner، ورسائل قابلة للتنفيذ |

لن نضع طلبات API داخل صفحات العرض مباشرة. كل شاشة ستستهلك hook أو service typed، حتى يمكن تبديل أسماء المسارات أو شكل الاستجابة في مكان واحد إذا ظهر اختلاف بين بيئة التطوير والإنتاج.

### إدارة الأداء

سيتم تحميل الشاشات الثقيلة بشكل lazy، وتقليل إعادة الجلب عبر cache محلي بسيط لكل domain، وتأخير البحث حتى يتوقف المستخدم عن الكتابة، وإلغاء الطلب السابق عند بدء طلب جديد. جداول الفواتير والمنتجات ستستخدم pagination server-side عندما يدعم endpoint ذلك، مع debounce للفلاتر. لن نستخدم polling إلا إذا احتاجت حالة الاشتراك أو الإشعارات ذلك فعلياً.

## 5. خريطة الشاشات وتجربة المستخدم

### منطقة التاجر `admin`

1. **تسجيل الدخول والتحقق والاستعادة:** مسار قصير، يعرض حالة الطلب ورسالة الخطأ من الباك إند بصياغة عربية مفهومة.
2. **لوحة التحكم:** إجمالي المبيعات، صافي الربح، عدد الفواتير، تنبيهات المخزون، مخطط المبيعات، وآخر الفواتير.
3. **نقطة البيع:** بحث سريع عن SKU/اسم المنتج، فئات جانبية، بطاقات منتجات، سلة RTL مثبتة، اختيار عميل، طريقة الدفع، تأكيد الفاتورة، ونتيجة قابلة للطباعة إذا كانت الاستجابة تدعمها.
4. **المنتجات:** جدول بحث وتصفية، إضافة/تعديل في Drawer، رفع صورة، عرض كمية المخزون، وحذف مع تأكيد واضح.
5. **الفئات:** إدارة خفيفة داخل صفحة المنتجات أو Drawer مستقل حسب كثافة البيانات.
6. **الفواتير:** جدول مع البحث بالتاريخ والعميل والمبلغ، صفحة تفاصيل، وحساب الربح.
7. **العملاء:** قائمة، إحصاءات، إضافة وتعديل، وصفحة تفاصيل.
8. **المخزون:** مؤشرات، قائمة منخفض المخزون، تقرير بفترة وفئة.
9. **التحليلات والتقارير:** فلاتر فترة، بطاقات ملخص، رسوم Recharts، وحالات عدم وجود بيانات.
10. **الاشتراك والملف الشخصي:** رفع إيصال الاشتراك، حالة الاشتراك، بيانات الشركة، وتغيير المعلومات المتاحة.

### منطقة السوبر أدمن `superAdmin`

لوحة منفصلة بنفس نظام التنقل لكن بألوان وظيفية أكثر تحفظاً، وتضم نظرة عامة على المستخدمين والتجار، إدارة الأدمنز، الاشتراكات المعلقة، الإشعارات، المنتجات والفواتير عبر المنصة، مع فلاتر `adminId` وحقول الفترة والمبلغ.

## 6. نظام التصميم

سنعتمد على خطي **IBM Plex Sans Arabic** للعناوين و**Noto Sans Arabic** للنصوص والبيانات. الخلفية عاجية دافئة، النص كحلي حِبري، البطاقات ذات ظلال ناعمة بدلاً من حدود كثيفة، والنحاسي `#B96F4A` للحركة الأساسية. التخطيط RTL يستخدم sidebar ثابتاً على اليمين في سطح المكتب وDrawer في الهاتف، مع منطقة سلة خاصة بنقطة البيع.

الحركة محدودة وذات معنى: ضغط الأزرار 100–160ms، النوافذ 180–240ms، وتظهر الصفوف بتدرج صغير. سيتم احترام `prefers-reduced-motion`. الأصول المرئية المولدة موجودة ضمن مسارات Manus storage ويمكن استخدامها للخلفية، نمط الإيصال، صورة التقارير، ورمز العلامة.

## 7. مراحل التنفيذ المقترحة

| المرحلة | الناتج | معيار القبول |
|---|---|---|
| 1. Foundation | RTL، الخطوط، الألوان، layout، routing، auth context، API client | الدخول والـ logout والحماية تعملان مع حالات loading/error |
| 2. Merchant shell | لوحة التاجر، sidebar، topbar، اختيار المسار، responsive | كل الصفحات الرئيسية قابلة للوصول دون dead ends |
| 3. POS core | المنتجات، البحث، السلة، إنشاء الفاتورة | يمكن تنفيذ بيع كامل من البحث حتى استجابة الباك إند |
| 4. Catalog & stock | المنتجات، الصور، الفئات، المخزون، منخفض المخزون | CRUD متوافق مع validation الفعلية وmultipart |
| 5. Invoices & customers | القوائم، التفاصيل، البحث، العميل، الأرباح | فلاتر الجداول وحالات الفراغ والأخطاء واضحة |
| 6. Analytics | dashboard summary، periods، compare، reports | الرسوم لا تفترض أرقاماً غير موجودة وتتعامل مع البيانات الفارغة |
| 7. Subscriptions | رفع الإيصال، حالة الاشتراك، شاشة superAdmin للمراجعة | الرفض يطلب سبباً، والرفع يعرض progress ونتيجة |
| 8. Hardening | اختبار المسارات، responsive، accessibility، build، performance | لا أخطاء TypeScript، ولا requests مكررة أو routes محمية مكشوفة |

## 8. مخاطر وقرارات يجب تثبيتها قبل التطوير الكامل

أولاً، يجب تأكيد عنوان الباك إند الفعلي وCORS واسم الحقول التي يعيدها login، خصوصاً شكل access token وrefresh token. ثانياً، يجب حسم ما إذا كانت نقطة البيع تنشئ الفاتورة عبر body محدد يحتوي على `products` أو `items`، لأن هذا هو العقد الأهم في النظام ولم يكفِ تعريف route وحده لتثبيته. ثالثاً، يجب تأكيد هل طباعة الإيصال مطلوبة في النسخة الأولى وهل يعيد endpoint رابطاً أو بيانات قابلة للطباعة.

كما يجب التحقق من اختلاف مسارات الاختبارات عن mounts الحالية، وتحديد هل `checkSubscription` يعيد 401 أو 403 مع تفاصيل حالة الاشتراك. هذه المعلومات ستحدد شاشة الـ paywall ورسالة إعادة التفعيل.

## 9. خطة الاختبار

سيتم اختبار كل service على استجابات النجاح والفشل، خصوصاً 401 و403 و422/400 و500. ستُراجع حماية role routes، refresh عند انتهاء التوكن، منع إرسال طلبات مكررة عند النقر السريع، وسلوك الواجهة عند انقطاع الشبكة. بصرياً ستُراجع نقطة البيع ولوحة التحكم على سطح مكتب وهاتف، مع دعم لوحة المفاتيح والتركيز المرئي واتجاه RTL.

## 10. التوصية التنفيذية

نبدأ بالـ Foundation ثم نبني **نقطة البيع** قبل التوسع في التقارير، لأنها المسار الذي يثبت صحة التكامل مع الباك إند مبكراً. بعد تثبيت عقد `add-invoices` وlogin، تُبنى بقية الشاشات على نفس طبقة الخدمات والنماذج. لا نوصي بإعادة كتابة الباك إند أو تغيير endpoints؛ أي اختلاف يُعالج داخل adapter في الواجهة مع توثيق واضح.
